# スコープ定義書 — prism (Prism Earring)

## In(今回やること)

| # | ケイパビリティ | 内容 | Source |
|---|---|---|---|
| 1 | DSP コア | `prism::PitchShifter` — 純 C++17・依存ゼロ。ディレイライン型(リングバッファ + 2 読み出しポインタ + 等パワークロスフェード)。ステレオ、左右独立 shift_cents(-150〜0、既定 -89)、dry_wet、crossfade_ms(10〜100、既定 50)を std::atomic で実行時可変。コールバック内はヒープ確保・ロック・I/O・例外なし | [Q1] [desc] |
| 2 | オフライン数値検証 | ピッチ精度(110/440/3520 Hz、入出力比 0.95±0.5%、検証側 FFT)、レイテンシ(インパルス、処理部 ≤10ms 相当サンプル数)、グリッチ(連続正弦波の不連続検出)、CPU 負荷(処理時間 / 実時間比)。clang++ のみでビルド・実行できる自動テスト | [Q1] [desc] |
| 3 | Web デモ | 同一 C++ ソースを Emscripten で WASM 化し AudioWorklet で実行。静的ページ 1 枚(マイク→シフト→出力、シフト量/ドライウェット/窓長スライダー、実測往復遅延表示)。Pixel Chrome / Mac ブラウザ対応。echoCancellation 等は無効化 | [Q1] [Q2] |

## Out(今回やらないこと)

- JUCE スタンドアロンアプリ(Web デモに置換)[Q2]
- Android ネイティブ版(段階3、将来の別ワークフロー)[Q2]
- 帯域分割(段階4、実使用後に判断)[Q2]
- デモ UI の見た目の作り込み [Q2]

## 順序と期限

- 実装順: DSP コア → オフライン検証 → Web デモ(依存順 = リスク先行)[Q3]
- 期限: なし(個人プロジェクト)[Q3]

## Assumptions & Open Questions

None.
