# Prism Earring

リポジトリ名: `prism-earring` ／ 名前空間: `prism`

プリズムが光を屈折させるように、耳に着けて音の高さを曲げる装置。
補装具であると同時に装身具である、という位置づけを名前に込めている。

## 背景

開発者本人が15年来、**すべての音が約89セント高く聞こえる**という症状を持つ。
実測値: 440Hz の純音を鳴らすと A♯ に聞こえ、418Hz まで下げると正しく A に聞こえる。

- 周波数比: 418 / 440 = **0.95**(ちょうど)
- セント換算: 1200 × log2(0.95) = **約 -88.8 セント**(半音 = 100セント なので、半音よりわずかに狭い)
- 再現性: 15年間一貫しており、日による変動はない
- **未確認**: 全帯域で一定比率か(110Hz / 3520Hz は未測定)、左右差があるか

## 目的

マイクで拾った外界の音をリアルタイムに約 -89 セントピッチシフトし、
イヤホンで聴くことで正しい音高で世界を聞けるようにする。

最終形態は Android アプリ(開発者は iPhone を所有していない)。

---

## 最重要制約: レイテンシ

**この制約がアルゴリズム選択を支配する。音質より優先する。**

イヤホンは外界音を完全には遮断しないため、生音が漏れ込む。処理音と生音が
重なった状態で遅延が大きいと、唸り・エコー・会話の破綻が起きる。

- 目標: 処理部の遅延 **10ms 以下**
- 往復(マイク→イヤホン)で 20ms 以下に収まらない端末では実用にならない

### したがって

**位相ボコーダ(phase vocoder)・FFT ベースの手法は使わない。**

窓長 2048 サンプル @ 44.1kHz = 46ms。この時点で要件を満たさない。
音質面では位相ボコーダが優れているが、本プロジェクトでは採用しない。

**採用するのはディレイライン型ピッチシフタ**(テープヘッド回転方式 /
delay-line pitch shifter):

- リングバッファに入力を書き込む
- 読み出しポインタを 0.95 倍速で走らせる(時間領域のリサンプリング)
- ポインタがバッファ端に到達する不連続を避けるため、**2本のポインタを
  位相を半周ずらして走らせ、クロスフェードする**
- 遅延はバッファ長に依存し、数ms に収まる

この方式の弱点はシフト量が大きいときのアーティファクト(金属的な響き・
ダブリング)だが、**本件はシフト量が -89 セントと極小のためほとんど問題に
ならない**。読み出しポインタの速度が 0.95 と 1.0 に近いため。

---

## 設計方針

### パラメータ(すべて実行時に可変)

| パラメータ | 範囲 | 既定値 | 備考 |
|---|---|---|---|
| shift_cents_L | -150 〜 0 | -89 | 左チャンネルのシフト量 |
| shift_cents_R | -150 〜 0 | -89 | 右チャンネル(複聴の可能性があるため独立) |
| dry_wet | 0.0 〜 1.0 | 1.0 | 原音とのミックス比 |
| crossfade_ms | 10 〜 100 | 50 | ディレイラインの窓長。音質と遅延のトレードオフ |

**測定を前工程として切り出さず、パラメータとして製品に畳み込む方針。**
実際の環境音を聞きながら耳で合わせ込む方が、純音測定より実使用条件に近い解が得られる。

### 段階

1. **PC / JUCE スタンドアロンアプリ** — マイク入力 → シフト → 出力。効果検証用
2. **DSP 部の切り出し** — JUCE 非依存の純粋な C++ クラスにする。Android 移植の準備
3. **Android 版** — Oboe(または AAudio 直叩き)。JUCE の Android オーディオ層は
   低レイテンシ用途では弱いため、ここは JUCE を使わない可能性が高い
4. **帯域分割**(必要なら) — 全帯域一律で違和感が残る場合のみ。実装コストが跳ねるため、
   実際に使ってから判断する。**最初から作らない**

---

## リアルタイムオーディオの鉄則

オーディオコールバック内で以下を **絶対にしない**:

- ヒープ確保 / 解放(new, delete, malloc, free, std::vector の伸長, std::string)
- ロック(mutex, lock_guard)
- ファイル I/O、ログ出力、printf
- システムコール全般
- 例外を投げる

パラメータの受け渡しは `std::atomic` またはロックフリーの FIFO を使う。
バッファは初期化時に確保し、以後サイズを変えない。

---

## 検証方法

**耳で確認するだけでは不十分。数値で検証できる形にする。**

1. **ピッチ精度**: 既知周波数の正弦波(110 / 440 / 3520 Hz)を入力し、
   出力を FFT してピークを取る。入出力比が 0.95 ± 0.5% に収まること
2. **レイテンシ**: インパルスを入力し、出力に現れるまでのサンプル数を数える
3. **グリッチ**: 連続正弦波を長時間流し、出力の不連続点を検出する
4. **CPU 負荷**: コールバックの実行時間を計測し、バッファ長に対する余裕を確認

これらは**オフライン(ファイル入出力)で自動テストとして回せる形にする**。
DSP 部を JUCE 非依存にしておく理由のひとつでもある。

---

## AI 支援時の注意(このリポジトリ固有)

過去に以下の誤りが繰り返し発生している。提案する前に確認すること:

- **位相ボコーダ / FFT ベースの提案** — 音質の観点では正解だが、本件では
  レイテンシ要件により却下される。上記「最重要制約」を参照
- **オーディオコールバック内でのメモリ確保** — 動くように見えるが、実機で
  ドロップアウトの原因になる
- **`juce::dsp` の高レベルクラスへの安易な依存** — Android 移植時に負債になる。
  DSP 部は素の C++ で書く
- **半音 = 100セント と決め打ちした実装** — 本件は 89セント。ハードコードしない

---

## 命名規約

- 名前空間: `prism`
- DSP コアクラス: `prism::PitchShifter`(JUCE 非依存)
- Android パッケージ ID: `dev.saku.prismearring`(衝突回避のため名前空間で逃がす)
- 表記ゆれを避けるため、コード中では `Prism Earring` ではなく `prism` / `PrismEarring` を使う

## 開発環境

- 言語: C++(C++17 以上)
- PC 段階: JUCE 8.x、Windows
- Android 段階: NDK、Oboe、Android Studio
- 開発者は業務で Unity / Unreal Engine / Wwise / CRI ADX 向けの
  クロスプラットフォーム C++ プラグイン開発に従事しており、
  ビルドフロー・NDK・Android 実機デバッグの経験がある。基礎的な説明は不要
